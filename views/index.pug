include mixins/head
include mixins/footer

+head('My Blog')

body
  h1 Blog
  p I am glad to see you here, on my blog page. 
  hr
  h3 Articles:
  ul
    each article in articles
      li: a(href='/article/'+article._id) #{article.title}

  +footer(user)